apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-nginx" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "my-nginx" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

