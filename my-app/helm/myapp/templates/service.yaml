apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: myapp-{{ .Values.color }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
